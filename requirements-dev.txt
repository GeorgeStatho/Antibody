# Test tooling. Not installed into the container image.
-r requirements.txt

# The fingerprint determinism tests are the Memory Bank's contract with the fleet.
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
