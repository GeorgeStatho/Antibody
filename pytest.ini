[pytest]
testpaths = tests
# tests/ is on the path so a test module can name the shared constants in conftest.
pythonpath = . tests
asyncio_mode = auto
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
